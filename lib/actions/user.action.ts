'use server'

export const signIn = async () => {
  try {
    //mutate the database
  } catch (error) {
    console.error('Error:', error);
    
  }
}

export const signUp = async ({userData}: SignUpParams) => {
    try {
      //mutate the database
    } catch (error) {
      console.error('Error:', error);
      
    }
  }